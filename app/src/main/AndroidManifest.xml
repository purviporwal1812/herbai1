<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.herbai">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Camera feature -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:theme="@style/Theme.HerbAi"
        android:label="HerbAi"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true"
        android:requestLegacyExternalStorage="true">

        <!-- File Provider -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.herbai.fileprovider"
            android:grantUriPermissions="true"
            android:exported="false">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PlantImageGalleryActivity"
            android:label="Plant Images"

            android:exported="false" />

        <activity
            android:name=".FullScreenImageActivity"
            android:label="Full Screen Image"
            android:exported="false" />

        <!-- Other Activities -->
        <activity
            android:name=".ResultActivity"
            android:exported="false" />
        <activity
            android:name=".FirstPageActivity"
            android:exported="false" />
        <activity
            android:name=".SecondPageActivity"
            android:exported="false" />
        <activity
            android:name=".ThirdPageActivity"
            android:exported="false" />
        <activity
            android:name=".PlantSearchActivity"
            android:exported="false"
            android:label="Plant Search" />
        <activity
            android:name=".PlantDetailActivity"
            android:exported="false"
            android:label="Plant Details" />
        <activity
            android:name=".ApiStatusActivity"
            android:exported="false"
            android:label="System Status" />
        <activity
            android:name=".GenerateDataActivity"
            android:exported="false"
            android:label="Generate Plant Data" />
        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:label="Settings" />

    </application>
</manifest>